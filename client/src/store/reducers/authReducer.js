const initState = {
  isLoggedIn: false,
  token: null
}

const authReducer = (state = initState, action) => {
  switch (action.Type) {
    default:
      return state
  }
}

export default authReducer
